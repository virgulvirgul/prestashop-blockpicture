# prestashop-blockpicture
Simple module for prestashop to display an image on the sidebar.

### Usage
Clone or download repository and move the folder `blockpicture` into the `prestashop/modules` folder.
